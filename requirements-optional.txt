# Optional dependencies. Nothing here is required to run the app
# (streamlit run app\streamlit_app.py) or the core test suite - install only
# the pieces you actually need.
#
#   pip install -r requirements-optional.txt      # everything below
#   pip install rasterio                            # just real raster I/O
#   pip install pandas PyYAML                       # just the RS-adaptation script

# --- Real raster I/O (GeoTIFF, real CRS/band metadata) ---
# Optional and guarded: src/ingestion/raster_io.py tries rasterio first and
# falls back to Pillow-based loading (PNG/JPEG, no CRS/band metadata) if it
# is not installed. Only needed for real satellite raster files.
rasterio>=1.3

# --- Used only by scripts/adapt_rs_vlm.py (the NOT-YET-EXECUTED remote-sensing
# LoRA adaptation script - see docs/rs_adaptation.md). Not used by app/ or src/. ---
pandas>=2.0        # reads/summarizes the VRSBench dataset manifest
PyYAML>=6.0         # loads configs/rs_adaptation.yaml
peft>=0.10          # LoRA adapter training
datasets>=2.18      # HuggingFace dataset loading for VRSBench

# --- Used only by scripts/second_success_test_grounding.py and
# src/models/florence2_compat.py's Florence-2 compatibility path
# (the evaluated-and-declined Florence-2-base grounding experiment -
# see docs/rs_adaptation.md and docs/RUN_ON_WINDOWS.md). Not used by the
# shipped app. ---
einops>=0.7          # Florence-2's custom modeling code imports this directly
timm>=0.9            # Florence-2's DaViT vision backbone is built on timm

# --- Used only by scripts/first_success_test_vqa.py and
# scripts/verify_environment.py (one-off hardware/verification scripts,
# not the shipped app). ---
psutil>=5.9          # cross-platform process RAM measurement

# --- Used only by the rejected deep-learning fusion/change candidates
# researched (not adopted) in docs/rs_adaptation.md sections 9-10. Not
# required for anything in this repo today. ---
torchvision>=0.17
